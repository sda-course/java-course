package java_course._1_basics.chapter08;

import java.util.Scanner;

public class Exercises {
    public static void main(String[] args) {
        /*
        1. Utwórz klasę Temperature w osobnym pliku. Zadeklaruj w niej pola:
         - 'value' typu double.
         - 'minValue' typu double
         - 'maxValue' typu double
         */


        /*
        2. W klasie Temperature zadeklaruj metodę 'isInRange()', która zwraca true, jeśli value mieści się między 'minValue' a 'maxValue' włącznie.
         */

        /*
        3. Utwórz poniżej zmienną typu Temperature o nazwie 'flueTemperature'. Zainicjuj pola obiektu wartościami:
        - value: 14.5
        - minValue: 0
        - maxValue: 100
         */

        /*
        4. Wyświetl wynik wywołania metody 'isInRange' dla obiektu 'flueTemperature'.
         */

        /*
        5. Zdefiniuj konstruktor dla klasy 'Temperature', który inicjuje tylko pole `value`, a polom `minValue` i `maxValue` nadaje wartości odpowiednio: 0, 100.
         */

        /*
        6. Zdefiniuj konstruktor bez argumentów dla klasy Temperature, który inicjuje tylko pola 'minValue' i 'maxValue' wartościami 0 i 100. Wykorzystaj zmienną this, aby
        odwołać się do istniejącego konstruktora.
        */

        /*
        7. Zmień modyfikatory dostępu do pól klasy, aby wszystkie były prywatne. Aby obecny program działał, zmień wykonane polecenie nr 3, w którym utwórz
        obiekt przy pomocy konstruktora z jednym argumentem. Usuń też wiersze z przypisaniem wartości do pól obiektu, które obecnie są prywatne.
         */
        /*
        8. Dodaj do klasy 'Temperature' gettery do każdego pola oraz seter do pola `value`.
         */

        /*
        9. Zdefiniuj klasę 'HumanTemperature' z identycznymi polami jak w klasie 'Temperature'. Pola 'minValue' i 'maxValue' zdefiniuj jako statyczne i zainicjuj je wartościami:
        21 i 42.
         */

        /*
        10. Zdefiniuj w klasie 'HumanTemperature' metodę 'isNormal()', która zwraca true, jeśli pole 'value' mieści między 36.0 a 37.0.
         */
    }
}
